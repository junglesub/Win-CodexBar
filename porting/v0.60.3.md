# Port upstream CodexBar v0.60.3

Status: implementation integrated; validation complete; ready for review.

This is a Windows behavior port. The integration PR kept the Win-CodexBar
product version at `0.56.8`; the release preparation commit advances the
product metadata to `0.60.3` after the port is integrated.

## Frozen boundaries

| Item | Value |
| --- | --- |
| Upstream repository | `steipete/CodexBar` |
| Previous upstream tag | `v0.60.2` (`d2d368c53444c1e1662d8d87d03fe02dc2b1f0a7`) |
| Target upstream tag | `v0.60.3` (`1e3d97083ed5a38634d9dd489237dc73210a5f8e`) |
| Win-CodexBar port base | `a4c3d7c25` (`port/upstream-0.60.2`) |
| Integration branch | `port/upstream-0.60.3` |
| Upstream release | `2026-09-15T09:18:37Z` |

The original checkout was left untouched because it contained unrelated dirty
work. This branch uses a dedicated worktree created from the accepted v0.60.2
port line.

## Upstream behavior matrix

The immutable `v0.60.2...v0.60.3` range contains 11 commits and 42 changed
files. Each behavior change was checked against the active Rust/Tauri/CLI
architecture.

| Upstream commit | Behavior | Final disposition |
| --- | --- | --- |
| `16e9c8ad1` | Keep Cursor monthly Auto pace separate from the Grok Bot extra allowance | PORT; Cursor Automatic explicitly selects the semantic monthly lane and excludes Grok Bot extras. Per-extra recurring pace is N/A because the Windows bridge has provider-level pace only |
| `6ffc81f6e` | Restore paid and active-trial Grok Bot usage and omit expired trials | PORT; Rust Cursor API maps `includedLimitZero` and `sandTrialExpiresAt` with deterministic tests |
| `769f26d50` | Disable Claude Remote Control registration for usage probes | PORT; Windows Claude probe passes `--settings {"remoteControlAtStartup":false}` |
| `b202fc0c8` | Keep weekly fallback data out of Claude session warnings | PORT; missing Claude OAuth sessions now use the shared informational placeholder, so the existing dispatcher skips session notifications, transitions, hooks, and predictive pace while the weekly lane remains eligible |
| `d55ead486` | Preserve known partial spend and unpriced coverage | ALREADY-COVERED; `SpendContract` retains known subtotals, explicit coverage counts, unknown daily values, and the `~` partial-cost display |
| `1517c2a4d` | Retain shared spend beside Claude account groups in the web dashboard | ALREADY-COVERED; the Windows dashboard and claude-swap projections keep provider-level spend separate from account-scoped usage |
| `d3610d095` | Explain Antigravity Auto account selection in the native settings picker | ADAPT; expose the existing Windows Auto and Local API / agy CLI sources with the account-identity guidance in the Tauri settings policy |
| `d581f659f` | Detect Homebrew-managed app bundles for Sparkle updates | MACOS-ONLY/N-A; Windows uses the Tauri updater and has no Homebrew bundle path |
| `9db44805d` | Update the macOS appcast for 0.60.3 | RELEASE-ONLY/N-A |
| `2b7816493` | Prepare upstream release notes | RELEASE-ONLY/N-A |
| `a5f2c581c` | Open upstream 0.60.3 development | RELEASE-ONLY/N-A |

The upstream Swift/AppKit, Sparkle, appcast, localization, and native web UI
fixtures remain outside the Windows port unless a Windows observable is
proven. No provider identity, account, cookie, or token data crosses provider
boundaries.

## Ported files

| File | Change |
| --- | --- |
| `rust/src/providers/cursor/api.rs` | Decode paid/trial Grok Bot fields; include active trials without recurring reset metadata; hide expired trials |
| `apps/desktop-tauri/src-tauri/src/usage_metric.rs` | Keep Cursor automatic selection on the semantic monthly lane while preserving explicit extra selection |
| `rust/src/providers/claude/mod.rs` | Opt usage probes out of Claude Remote Control registration and assert the exact argv contract |
| `rust/src/providers/claude/oauth/mod.rs` | Represent a missing OAuth session as an informational lane so warning state is preserved |
| `rust/src/providers/claude/oauth/tests.rs` | Regress missing-session preservation while keeping weekly usage available |
| `rust/src/notifications.rs` | Keep informational Claude session lanes out of warning history, hooks, and predictive pace consumers |
| `apps/desktop-tauri/src-tauri/src/commands/providers.rs` | Route session notification checks through the informational-lane guard while retaining weekly checks |
| `apps/desktop-tauri/src/surfaces/settings/providers/sections/usageSourcePolicy.ts` | Expose Antigravity Auto and Local API / agy CLI with the account-selection guidance |
| `apps/desktop-tauri/src/surfaces/settings/providers/sections/UsageSourceSection.test.tsx` | Assert the Antigravity source options and identity warning |

## Validation gates

The focused and repository-wide gates pass:

- Cursor provider tests: 35 passed
- Claude provider tests: 142 passed, 1 ignored
- Notification tests: 12 passed
- Tauri usage-metric tests: 13 passed
- Frontend tests: 61 files, 359 tests passed
- `pnpm run build`: passed
- `./scripts/local-check.ps1 -Slice ci`: passed Rust format, workspace clippy,
  1,821 Rust tests, frontend tests/build, and interaction guard checks
- Fresh `pnpm --dir apps/desktop-tauri run tauri:build:debug`: passed
- CUA proof: [Antigravity source policy screenshot](v0.60.3-antigravity-source-policy-scrolled.png)
  shows the Auto and Local API / agy CLI choices plus the account-identity
  guidance in the freshly built Windows settings surface.
